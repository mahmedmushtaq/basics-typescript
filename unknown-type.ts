let value:unknown;


value  =2;
value = 'max';

// but unknow cannot be assign directly e.g

let stringValue:string;

//stringValue = value; // in order to assign stringValue=value must be check typeof of value 
//and then assign or if value has any type then it must be assign easily